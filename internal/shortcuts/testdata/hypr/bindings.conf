# User overrides
unbind = SUPER SHIFT, V
bind = SUPER_SHIFT, u, exec, beam send  # Beam, do not touch
bindd = SUPER ALT, U, Something, exec, notify-send "a, b"
source = ./hyprland.conf

submap = resize
bind = SUPER SHIFT, V, exec, never-counts
submap = reset
