# User overrides
unbind = SUPER ALT, V
bind = SUPER_ALT, u, exec, beam send  # Beam, do not touch
bindd = SUPER SHIFT, U, Something, exec, notify-send "a, b"
source = ./hyprland.conf

submap = resize
bind = SUPER ALT, V, exec, never-counts
submap = reset
